package main

func main() {
	/*
		cert, err := tls.LoadX509KeyPair("cert.pem", "key.pem")
		if err != nil {
			log.Fatal(err)
		}

		secret, err := v1.Pair("192.168.1.20", 6467, &cert)
		if err != nil {
			log.Fatal(err)
		}
		fmt.Println(secret)
	*/
	secret := "="
}
